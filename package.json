{
  "name": "openlog",
  "version": "0.1.0",
  "private": true,
  "description": "Open log console for mobile debugging",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "start": "node packages/server/bin/openlog"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "turbo": "^1.11.0",
    "typescript": "^5.3.0",
    "vite-plugin-css-injected-by-js": "^4.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
